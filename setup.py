from setuptools import setup

setup(name='Librarian',
      version='0.1',
      description='Set of spiders used to collect data for search engine, Alexandria.',
      license='MIT',
      packages=['Bots'],
      zip_safe=False)